{
  "name": "pyra",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:web": "pnpm --filter @pyra/web dev",
    "dev:worker": "pnpm --filter @pyra/worker dev",
    "build": "pnpm --filter @pyra/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @pyra/db db:generate",
    "db:migrate": "pnpm --filter @pyra/db db:migrate",
    "db:push": "pnpm --filter @pyra/db db:push",
    "prepare": "husky"
  },
  "devDependencies": {
    "@neon/config": "^1.4.2",
    "@neon/env": "^1.3.2",
    "@typescript-eslint/eslint-plugin": "^8.70.0",
    "@typescript-eslint/parser": "^8.70.0",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.3.3",
    "typescript": "^7.0.2"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*.{json,css,md}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  }
}
